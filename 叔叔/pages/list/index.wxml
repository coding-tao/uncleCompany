<!--pages/list/index.wxml-->
    <view class='center-wrap'>
        <view class='center-list' wx:for="{{listArr}}"  wx:key="{{item}}">
            <image src="{{item.img}}"></image>
            <view>{{item.content}}</view>
        </view>        
    </view>